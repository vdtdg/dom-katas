# Selecteur id

**Objectif :**  
Apprendre a cibler un element unique avec `#id`.

**Consignes :**
- Dans `style.css`, ajoute une regle `#titre-principal`.
- Dans cette regle, applique `color: #1d3557;`.

**Indice :**
- Le selecteur `#...` correspond a l'attribut `id="..."` en HTML.
