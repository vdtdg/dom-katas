# Head et title

**Objectif :**  
Placer les informations de page dans `<head>`.

**Consignes :**
- Ajoutez une balise `<meta charset="UTF-8">` dans `<head>`.
- Ajoutez un `<title>` avec le texte `Kata 34 - Head et title`.
