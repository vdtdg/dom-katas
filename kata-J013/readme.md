# Evenement input

**Objectif :**  
Reagir pendant la saisie dans un champ.

**Consignes :**
- Selectionnez `#nameInput` et `#preview`.
- Sur l'evenement `input`, copiez la valeur saisie dans `#preview`.
