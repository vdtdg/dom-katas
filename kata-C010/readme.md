# Etat hover

**Objectif :**  
Decouvrir un etat interactif simple avec `:hover`.

**Consignes :**
- Ajoutez une regle `.btn:hover` dans `style.css`.
- Dans cette regle, applique `background: #005fcc;`.

**Indice :**
- `:hover` s'active quand la souris passe au-dessus de l'element.
