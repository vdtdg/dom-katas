# id et class ensemble

**Objectif :**  
Combiner un `id` unique avec une `class` partagee.

**Consignes :**
- Ajoutez `id="infos"` sur la `<section>`.
- Ajoutez `class="note"` sur les trois `<p>`.

**Indice :**
- `id` cible la section completez, `class` cible les paragraphes en groupe.
