# id et class ensemble

**Objectif :**  
Combiner un `id` unique avec une `class` partagee.

**Consignes :**
- Ajoute `id="infos"` sur la `<section>`.
- Ajoute `class="note"` sur les trois `<p>`.

**Indice :**
- `id` cible la section complete, `class` cible les paragraphes en groupe.
